name: Request platform catalogue publication
on:
  push:
    branches: [master]
    paths:
      - 'workflows/**'
      - 'catalog.json'
      - 'catalog/assets/**'
      - '.github/workflows/website-content.yml'
  workflow_dispatch:
permissions: {}
concurrency:
  group: website-content-notification
  cancel-in-progress: false
jobs:
  notify:
    if: github.ref == 'refs/heads/master' && vars.WEBSITE_CMS_ENABLED == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Obtain token restricted to the platform repository
        id: app
        uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349
        with:
          app-id: ${{ vars.WEBSITE_CMS_APP_ID }}
          private-key: ${{ secrets.WEBSITE_CMS_APP_PRIVATE_KEY }}
          owner: Hearmeman24
          repositories: hearmemanai-platform
          permission-actions: write
      - name: Request central catalogue PR and publication
        env:
          GH_TOKEN: ${{ steps.app.outputs.token }}
          SOURCE_REPOSITORY: ${{ github.repository }}
          SOURCE_SHA: ${{ github.sha }}
        run: |
          gh workflow run publish-template.yml --repo Hearmeman24/hearmemanai-platform --ref main -f "source_repository=$SOURCE_REPOSITORY" -f "source_sha=$SOURCE_SHA"
          echo "Publication requested. Follow the central run for PR checks and live download verification:" >> "$GITHUB_STEP_SUMMARY"
          echo "https://github.com/Hearmeman24/hearmemanai-platform/actions/workflows/publish-template.yml" >> "$GITHUB_STEP_SUMMARY"
